{
  "name": "salesbot-root",
  "version": "1.0.0",
  "description": "Root package.json wrapper for Render deployment",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate",
    "build": "cd backend && npx prisma generate && npx prisma db push",
    "start": "cd backend && npx prisma db push --accept-data-loss && node src/server.js"
  }
}
