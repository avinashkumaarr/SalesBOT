{
  "name": "salesbot-root",
  "version": "1.0.0",
  "description": "Root package.json wrapper to prevent Render deploy errors when Root Directory is not set to backend",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx prisma db push",
    "start": "cd backend && node src/server.js"
  }
}
